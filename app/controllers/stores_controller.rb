class StoresController < ApplicationController
    # if logged_in?
    #     @stores = @current_user.stores
    #   else
    #     @stores = Store.all # or force a login
    #   end

        # if session[:user_id]
    #     @user = User.find(session[:user_id])
    #     @ = @user.songs
    #   else
    #     @songs = Song.all # or force a login
    #   end
    def index
    end
end
